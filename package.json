{
  "name": "local-llm-setup-docs",
  "private": true,
  "scripts": {
    "dev": "docmd dev",
    "build": "docmd build",
    "validate": "docmd validate"
  },
  "devDependencies": {
    "@docmd/core": "^0.9.2"
  }
}
